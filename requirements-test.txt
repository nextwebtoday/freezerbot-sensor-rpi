# Test dependencies
# Note: Hardware-specific packages (RPi.GPIO, gpiozero, w1thermsensor, pisugar)
# are NOT installed in CI. Tests must mock these imports.
Flask==3.1.0
requests==2.32.3
python-dotenv~=1.0.1
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-mock>=3.10
